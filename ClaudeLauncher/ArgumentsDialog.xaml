<Window x:Class="ClaudeLauncher.ArgumentsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Launch with Arguments"
        Width="400"
        Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Claude Arguments:"
                   Foreground="#E0E0E0"
                   FontSize="13"
                   Margin="0,0,0,8"/>

        <TextBox x:Name="ArgumentsBox"
                 Grid.Row="1"
                 Background="#2D2D2D"
                 Foreground="#E0E0E0"
                 BorderBrush="#3C3C3C"
                 CaretBrush="#E0E0E0"
                 Padding="8,6"
                 FontSize="13"/>

        <TextBlock Grid.Row="2"
                   Text="Examples: --resume, --continue, --model sonnet"
                   Foreground="#808080"
                   FontSize="11"
                   Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Width="80"
                    Padding="8,6"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Launch"
                    Width="80"
                    Padding="8,6"
                    IsDefault="True"
                    Click="Launch_Click"/>
        </StackPanel>
    </Grid>
</Window>
